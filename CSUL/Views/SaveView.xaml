<UserControl x:Class="CSUL.Views.SaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSUL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="1078">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Margin="10">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/Icons/map_black.png" Height="100"/>
                    <Image Grid.Column="1" Source="/Resources/Icons/save.png" Height="100"/>
                </Grid>
                <Label Content="将存档拖入此处导入，或点击导入存档" FontSize="15px" Foreground="{DynamicResource BlackBrush}"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="1" Margin="10">
            
        </Border>
    </Grid>
</UserControl>
