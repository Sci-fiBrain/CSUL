<UserControl x:Class="CSUL.Views.ModView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CSUL.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Visibility="Visible">
            <Border.Background>
                <SolidColorBrush Color="{DynamicResource BlackColor}" Opacity="0.5"/>
            </Border.Background>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                    <Label Content="BepIn" FontSize="70px" Foreground="{DynamicResource WhiteBrush}" Margin="0" Padding="0"/>
                    <Label Content="Ex" FontSize="70px" Foreground="#FFFF7500" Margin="0" Padding="0"/>
                </StackPanel>
                <Label Content="未安装BepInEx" FontSize="25px" Foreground="{DynamicResource WhiteBrush}" HorizontalContentAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
